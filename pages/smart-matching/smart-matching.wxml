<view class="chat-container">
  <scroll-view 
    class="chat-messages" 
    scroll-y 
    scroll-top="{{scrollTop}}"
    scroll-with-animation
    enable-back-to-top
  >
    <view class="message-list">
      <view 
        wx:for="{{messages}}" 
        wx:key="timestamp" 
        class="message {{item.role === 'user' ? 'user-message' : 'ai-message'}}"
      >
        <view class="message-avatar">
          <image 
            wx:if="{{item.role === 'user'}}" 
            src="{{avatarUrl}}" 
            mode="aspectFill" 
            class="avatar-img"
          />
          <image 
            wx:else
            src="https://files.homesee.xyz/api/files/download/IMG_20251208_160325.png"
            mode="aspectFill"
            class="avatar-img"
          />
        </view>

        <view class="message-content">
          <view class="message-bubble">
            <block wx:if="{{item.role === 'assistant'}}">
              <block wx:for="{{item.segments}}" wx:for-item="seg" wx:for-index="segIdx" wx:key="segIdx">
                <rich-text
                  wx:if="{{seg.type === 'html'}}"
                  nodes="{{seg.html}}"
                  class="rich-content"
                ></rich-text>
                <view
                  wx:elif="{{seg.type === 'link'}}"
                  class="ai-link {{seg.valid ? '' : 'ai-link-disabled'}}"
                  catchtap="onAssistantLinkTap"
                  data-valid="{{seg.valid}}"
                  data-path="{{seg.miniPath}}"
                  data-switchtab="{{seg.useSwitchTab}}"
                  data-external="{{seg.externalUrl}}"
                  data-reason="{{seg.reason}}"
                >
                  <text class="ai-link-label">{{seg.label}}</text>
                  <text wx:if="{{!seg.valid && seg.reason}}" class="ai-link-reason">（{{seg.reason}}）</text>
                </view>
              </block>
            </block>
            <text wx:else user-select>{{item.content}}</text>
          </view>
          <view wx:if="{{item.role === 'assistant' && item.cards && item.cards.length}}" class="ai-cards">
            <view
              wx:for="{{item.cards}}"
              wx:key="id"
              class="ai-card"
              data-id="{{item.id}}"
              bindtap="onCardTap"
            >
              <image class="ai-card-cover" src="{{item.cover}}" mode="aspectFill" />
              <view class="ai-card-top">
                <text class="ai-card-title">{{item.title}}</text>
                <text class="ai-card-price">¥{{item.price}}/月</text>
              </view>
              <text class="ai-card-meta">{{item.meta}}</text>
              <view class="ai-card-actions">
                <view class="ai-card-btn ghost" data-id="{{item.id}}" catchtap="onCardVrTap">VR看房</view>
                <view class="ai-card-btn" data-id="{{item.id}}" catchtap="onCardAppointmentTap">预约看房</view>
              </view>
            </view>
          </view>
        </view>
      </view>

      <!-- 加载中 -->
      <view wx:if="{{loading}}" class="message ai-message">
        <view class="message-avatar">
          <image 
            src="https://files.homesee.xyz/api/files/download/IMG_20251208_160325.png"
            mode="aspectFill"
            class="avatar-img"
          />
        </view>
        <view class="message-content">
          <view class="message-bubble typing-indicator">
            <view class="dot"></view>
            <view class="dot"></view>
            <view class="dot"></view>
          </view>
        </view>
      </view>
    </view>
  </scroll-view>

  <view class="chat-input-area">
    <view class="input-wrapper">
      <input 
        class="chat-input" 
        value="{{userInput}}" 
        bindinput="handleInput" 
        bindconfirm="sendMessage"
        confirm-type="send"
        placeholder="请输入您的租房需求..."
      />
      <button 
        class="send-btn {{!userInput || loading ? 'disabled' : ''}}" 
        bindtap="sendMessage"
        disabled="{{!userInput || loading}}"
      >
        发送
      </button>
    </view>
  </view>
</view>
